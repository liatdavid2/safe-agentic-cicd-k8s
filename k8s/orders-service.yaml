apiVersion: v1
kind: Service
metadata:
  name: orders-api
  namespace: agentic-devops
spec:
  type: ClusterIP
  selector:
    app: orders-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
